# Zworker contributor entrypoint

Read and follow `AGENTS.md`. For the CNT private fork, also read:

- Public branding is **Zworker** and the operator UI is Russian-first. Preserve the compatibility identifiers explicitly listed in `AGENTS.md`.
- Use `ui/public/zworker-logo.png`, keep Russian catalog coverage current, and run `pnpm check:zworker-brand` for user-facing changes.

- `doc/CNT-SELF-HOSTING.md`
- `doc/UPSTREAM-SYNC.md`
- `doc/FORK-RELEASE.md`

Use `.agents/skills/sync-upstream/SKILL.md` for upstream merges and
`.agents/skills/deploy-cnt/SKILL.md` for production releases.
